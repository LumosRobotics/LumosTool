 * User shall specify either board or MCU, not both.
 * If board is specified, MCU is inferred from the board.
 * Include external libraries, sources, object files
 * Specify compiler and linker flags
 * Be able to specify memory regions, and place functions and data in those regions
 * Want to keep platform open, but also be able to build board specific projects
 * Flash with "lumos flash"